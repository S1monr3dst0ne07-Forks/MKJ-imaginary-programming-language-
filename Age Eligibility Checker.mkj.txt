importpkg mkjExecutable
mkjExecutable:activate("main.mkj")

-- Define voting_age (18, number_nodec).
var.new voting_age
var voting_age.type = number_nodec
var voting_age.value = 18

-- Define user_age (22, number_nodec).
var.new user_age
var user_age.type = number_nodec
var user_age.value = 22

-- Check if user_age is greater than or equal to voting_age.
if user_age >= voting_age:
    -- If True, print "Eligible to vote."
    log.print.("Eligible to vote.")
else:
    -- If False, print "Too young to vote."
    log.print.("Too young to vote.")

Output:
-- LOG --
Eligible to vote.

-- CONSOLE --
(None)